<header>
  <nav class="navbar bg-dark-subtle navbar-expand-lg">
    <div class="container-fluid">
      <%= link_to "Blog", root_path, class: "navbar-brand" %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <div class="navbar-nav ms-auto mb-2 mb-lg-0 gap-2">
          <% if user_signed_in? %>
            <div class="nav-item">
              <%= link_to "Sign Out", destroy_user_session_path, data: { turbo_method: 'delete' }, class: "btn btn-sm btn-link" %>
            </div>
            <div class="nav-item">
              <%= link_to "My Profile", profile_path, class: 'btn btn-sm btn-link' %>
            </div>
            <div class="nav-item">
              <%= link_to "Create", new_post_path, class: "btn btn-sm btn-primary" %>
            </div>
          <% else %>
            <div class="nav-item">
              <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-sm btn-primary" %>
            </div>
            <div class="nav-item">
              <%= link_to "Sign In", new_user_session_path, class: "btn btn-sm btn-secondary" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </nav>
</header>